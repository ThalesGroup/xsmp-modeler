# XSMP TAS-MDK

This extension packages the TAS-MDK XSMP contribution separately from the main `XSMP Modeler` extension.

It requires `XSMP Modeler` to be installed, then contributes the TAS-MDK profile through the XSMP extension manifest.
