assembly Demo

configure unsafe Child
{
    count = 1i32
}

Root: demo.Root
{
    child += Child: demo.Child
    field link unsafe outValue -> unsafe Child.inValue
    event link outbound -> unsafe Child.inbound
}
